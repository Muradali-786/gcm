enum UserAuthStatus { pending, approved, rejected }

enum StatusType { approved, rejected, pending }

enum ActionButtonType { approve, reject, primary, other }

enum TableColumnType { text, number, date, time }
